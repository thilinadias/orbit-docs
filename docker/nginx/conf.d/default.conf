server {
    listen 80;
    listen [::]:80;
    server_name _;
    root /var/www/public;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";

    index index.php;

    charset utf-8;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    error_page 404 /index.php;

    # Custom maintenance page for 502/503 (app container still starting up)
    error_page 502 503 /maintenance.html;
    location = /maintenance.html {
        root /etc/nginx/maintenance;
        internal;
    }

    location ~ \.php$ {
        fastcgi_pass app:9000;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_read_timeout 3600;
        fastcgi_connect_timeout 10s;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}

# SSL Configuration (Uncomment/Template if SSL is active)
# server {
#     listen 443 ssl default_server;
#     listen [::]:443 ssl default_server;
#     server_name _;
#     root /var/www/public;
#
#     ssl_certificate /etc/nginx/ssl/orbitdocs.crt;
#     ssl_certificate_key /etc/nginx/ssl/orbitdocs.key;
#
#     # ... rest of config
# }
